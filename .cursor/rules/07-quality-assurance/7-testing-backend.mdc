---
description: Apply backend testing standards for maintainability and reliability when writing backend tests. Focus on setup/teardown hooks and avoiding repetition.
globs: apps/backend/**/*.spec.*, apps/backend/**/*.test.*
alwaysApply: false
---

Setup and Teardown:
- Use setup hooks (`beforeEach`, `beforeAll`).
- Use teardown hooks (`afterEach`, `afterAll`).

Code Quality:
- Avoid code repetition in tests.
- Keep tests independent and focused.

